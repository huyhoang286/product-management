header.header
  // TOP BAR
  .top-bar
    .container.d-flex.justify-content-between
      .top-bar-left
        a(href="")
          i.fas.fa-phone-alt
          |  0867644926
        a(href="mailto:support@sapo.vn" class="d-none d-md-inline")
          i.fas.fa-envelope
          |  huyhoang.m28@gmail.com
      .top-bar-right.d-flex.align-items-center
        a(href="#")
          i.fas.fa-box
          |  Kiểm tra đơn hàng

  // MAIN HEADER
  .main-header
    .container
      .row.align-items-center
        // Logo
        .col-lg-3.col-md-12.text-center.text-lg-left.mb-3.mb-lg-0
          .d-flex.align-items-center.justify-content-center.justify-content-lg-start
            span.logo-text
              | SHOP 
              span.logo-orange ABC
            // SVG Icon bồn tắm
            svg(width="40" height="30" viewBox="0 0 24 24" fill="none" stroke="#d35400" stroke-width="1.5" class="ml-2")
              path(d="M2 12h20a2 2 0 0 1 2 2v2a6 6 0 0 1-6 6H6a6 6 0 0 1-6-6v-2a2 2 0 0 1 2-2z")
              path(d="M7 12V7a2 2 0 0 1 2-2h6a2 2 0 0 1 2 2v5")

        // Search
        .col-lg-6.col-md-8
          form(action="/search" method="GET")
            .input-group.search-group
              input.form-control(type="text" name="keyword" placeholder="Tìm kiếm sản phẩm")
              .input-group-append
                button.btn(type="submit")
                  i.fas.fa-search

        // Icons
        .col-lg-3.col-md-4.header-actions.d-flex.justify-content-end
          if (user)
            a.header-action-item.text-decoration-none.text-dark(href="/user/info")
              i.far.fa-user.text-success
              span.font-weight-bold.text-success #{user.fullName}
          else
            a.header-action-item.text-decoration-none.text-dark(href="/user/login")
              i.far.fa-user
              span Đăng nhập
          .header-action-item
            i.far.fa-heart
            span.badge-count 0
            span Yêu thích
          a.header-action-item.text-decoration-none.text-dark(href="/cart")
            i.fas.fa-shopping-bag
            span.badge-count #{miniCart ? miniCart.totalQuantity : 0}
            span Giỏ hàng

  // NAVIGATION
  nav.navbar.navbar-expand-lg.navbar-light
    .container
      button.navbar-toggler(type="button" data-toggle="collapse" data-target="#mainNav")
        span.navbar-toggler-icon
      .collapse.navbar-collapse#mainNav
        ul.navbar-nav.mr-auto
          li.nav-item(class=(path === '/' ? 'active' : ''))
            a.nav-link(href="/") Trang chủ
            
          li.nav-item(class=(path === '/about' ? 'active' : ''))
            a.nav-link(href="/about") Giới thiệu

          li.nav-item(class=(path && path.startsWith('/products') ? 'active' : ''))
            a.nav-link(href="/products") Sản phẩm
            
          li.nav-item(class=(path === '/news' ? 'active' : ''))
            a.nav-link(href="/news") Tin tức
            
          li.nav-item(class=(path === '/contact' ? 'active' : ''))
            a.nav-link(href="/contact") Liên hệ
            
          li.nav-item(class=(path === '/faq' ? 'active' : ''))
            a.nav-link(href="/faq") Câu hỏi thường gặp
            
          li.nav-item(class=(path === '/stores' ? 'active' : ''))
            a.nav-link(href="/stores") Hệ thống